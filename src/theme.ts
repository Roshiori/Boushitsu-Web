import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    // TODO:テーマ設定を行います
    palette: {
        primary: {main: "#00756A"},
        info: {main: "#349084"},
    }
});

export default theme;